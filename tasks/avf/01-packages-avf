# So we can make our loopback image have partitions
host_packages+=("kpartx")

# LVM ftw
host_packages+=("lvm2")

# Need some compression
host_packages+=("xz-utils")

# direct-root is evil
packages+=("sudo")
